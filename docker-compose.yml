services:
  cloudcode:
    build:
      context: .
      dockerfile: Dockerfile.platform
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped
